apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-aks-demo.fullname" . }}
  labels:
    {{- include "go-aks-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "go-aks-demo.selectorLabels" . | nindent 4 }}